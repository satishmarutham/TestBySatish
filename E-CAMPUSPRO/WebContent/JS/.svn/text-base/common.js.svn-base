$(document).ready(
		function() {
			

			$('table.displayTableClass th,.exportlinks').attr('class',
					'HometblHeader');
			/*
			 * $('.pagelinks').after('<br/>'); $('.pagebanner').css({ 'color' :
			 * '#000', 'font-weight' : 'bold', 'color' : '#299f9e', 'font-size' :
			 * '14px' }); $('.pagebanner').parent().css({ 'text-align' :
			 * 'center' });
			 */
			/*
			 * $('table.displayTableClass tr').find('th').attr('class',
			 * 'HometblHeader');
			 */
			setInterval(function() {
				$(".successmessagediv").hide();
			}, 7000);
			setInterval(function() {
				$(".errormessagediv").hide();
			}, 10000);
			
		/*	$('.pagebanner').hide();*/
			var listSize = $('.pagelinks').text();

			if (listSize <= 10) {
				$('.pagelinks').text("Page : " + listSize);
			} else {
			}
		});
